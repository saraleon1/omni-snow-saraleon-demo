# Reference this view as omni_dbt__supplies
schema_label: ""

schema: omni_dbt
table_name: SUPPLIES

dimensions:
  supply_uuid:
    sql: '"SUPPLY_UUID"'

  supply_id:
    sql: '"SUPPLY_ID"'
    format: ID

  product_id:
    sql: '"PRODUCT_ID"'
    format: ID

  supply_name:
    sql: '"SUPPLY_NAME"'

  supply_cost:
    sql: '"SUPPLY_COST"'

  is_perishable_supply:
    sql: '"IS_PERISHABLE_SUPPLY"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: supplies
  target_schema: dbt_sleon_prod
  config:
    materialized: table
  code: |-
    with

    supplies as (

        select * from {{ ref('stg_supplies') }}

    )

    select * from supplies
