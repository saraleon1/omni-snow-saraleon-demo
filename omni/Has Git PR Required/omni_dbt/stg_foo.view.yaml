# Reference this view as omni_dbt__stg_foo
schema_label: ""

schema: omni_dbt
table_name: STG_FOO

dimensions:
  mycol: {}

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_foo
  target_schema: dbt_sleon_prod
  config:
    materialized: view
  code: |-
    select 1 as "mycol"
    union
    select 2 as "mycol"
