# Reference this view as dbt_sleon_prod__example
schema: DBT_SLEON_PROD
table_name: EXAMPLE

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: example
  target_schema: dbt_sleon_prod
  config:
    materialized: table
  code: select 1 as id
