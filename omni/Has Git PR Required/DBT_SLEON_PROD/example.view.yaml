# Reference this view as dbt_sleon_prod__example
schema: DBT_SLEON_PROD
table_name: EXAMPLE

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

measures:
  count:
    aggregate_type: count
